import { createTheme } from '@mui/material'

const theme = createTheme({
    palette: {
        text:{
            black: 'rgb(0, 0, 0)',
            red: 'rgb(255,0,0)'
        }
    }
})

export default theme